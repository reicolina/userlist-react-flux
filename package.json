{
  "name": "my-user-list",
  "version": "0.1.0",
  "description": "My User List: An attempt to build an application using ReactJS and following the Flux architectural pattern",
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "gulp": "^3.8.7",
    "gulp-browserify": "^0.5.0",
    "gulp-concat": "^2.2.0",
    "react": "^0.11.1",
    "reactify": "^0.14.0",
    "flux": "^2.0.1"
  },

  "scripts": {
    "start": "gulp build && open dist/index.html && gulp watch",
    "build": "gulp"
  }
}
